import { DigiLayoutBlock } from "@digi/arbetsformedlingen-react";
import styled from "styled-components";

export const Header = styled(DigiLayoutBlock)`
  &&& {
    --digi--layout-block--background--primary: green;
  }
`